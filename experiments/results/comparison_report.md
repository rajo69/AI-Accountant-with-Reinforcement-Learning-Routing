# Routing Policy Comparison Report — SUPERSEDED

*Historical note. This file formerly carried a manually-written narrative of
the 2026-03-26 evaluation run, including claims such as "PPO variants beat the
baseline on the metrics that matter most" and "the quality of the confidence
signal is the binding constraint." Subsequent statistical rigour work (Wilson
95% CIs + two-proportion z-tests, commit 441d174) and the calibration and
regime probes (commits 9baba1c, 04f3c2e, and the regime-probe commits that
followed) showed those claims to be either statistically unsupported at
n=177, or directly contradicted by the probe evidence.*

The authoritative current sources for evaluation results are:

- **Raw numbers** (regenerated by `python -m agent.evaluate [--dataset ...]`):
  - `evaluation_results.json`             — raw Claude Haiku confidences
  - `evaluation_results_calibrated.json`  — Platt-scaled confidences
  - `evaluation_results_regime.json`      — calibrated with easy tier reshaped to 0.72
  - `evaluation_results_regime_raw.json`  — raw with easy tier reshaped to 0.72

- **Statistical summaries** (regenerated by
  `python -m experiments.statistical_analysis [--dataset ...]`):
  - `statistical_summary.md` / `.json`             — raw
  - `statistical_summary_calibrated.md` / `.json`  — calibrated
  - `statistical_summary_regime.md` / `.json`      — calibrated regime probe
  - `statistical_summary_regime_raw.md` / `.json`  — raw regime probe

- **Narrative**: the repository [README.md](../../README.md) Results section
  and the 1-page technical note at [paper/one_pager.md](../../paper/one_pager.md).

This placeholder is retained so external references to
`experiments/results/comparison_report.md` resolve to a page that explains
the change, rather than a stale conflicting narrative.
